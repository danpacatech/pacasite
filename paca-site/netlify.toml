[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# Friendly redirect so /admin and /admin/ both reach the CMS
[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301
[functions]
  directory = "netlify/functions"